# AutoParts Pro - Интернет-магазин автозапчастей

Современный интернет-магазин автозапчастей с архитектурой MVC.

## Технологии

- **Frontend:** Bootstrap 5, AdminLTE 3
- **Design System:** Футуристический дизайн с Glassmorphism
- **Backend:** PHP 8.2+ (MVC архитектура)
- **Database:** MySQL 8.0
- **Database Admin:** Adminer (веб-панель)
- **Containerization:** Docker & Docker Compose

## Дизайн-система

Проект использует единую систему дизайна для создания современного футуристического интерфейса.

**Основные принципы:**
- 🌙 Темная тема с акцентами (основной сайт)
- 💻 Светлая тема VS Code стиль (админ-панель)
- 🔮 Glassmorphism эффекты
- ✨ Градиенты и свечение
- 🎨 Единая цветовая палитра
- 📱 Полная адаптивность

Подробная документация: [DESIGN_SYSTEM.md](./DESIGN_SYSTEM.md)

## Структура проекта

```
/
├── app/                    # Приложение
│   ├── controllers/        # PHP контроллеры
│   │   ├── BaseController.php
│   │   └── AdminController.php
│   ├── models/             # PHP модели
│   │   └── BaseModel.php
│   └── config/             # Конфигурация
│       └── config.php
├── views/                  # HTML представления
│   ├── index.html         # Главная страница
│   └── admin/             # Админ-панель
│       ├── layout.html    # Layout для админки
│       └── dashboard.html # Дашборд
├── database/              # База данных
│   ├── init/             # SQL скрипты инициализации
│   └── migrations/       # Миграции
├── public/                 # Публичные ресурсы
│   ├── css/
│   │   ├── style.css      # Стили для сайта
│   │   ├── admin.css      # Стили для админки (старый)
│   │   ├── admin-vscode.css # Стили VS Code (новый)
│   │   └── variables.css  # Переменные дизайн-системы
│   └── js/
│       ├── main.js        # JS для сайта
│       └── admin.js       # JS для админки
├── index.php              # Точка входа (роутер)
├── Dockerfile             # Docker образ для приложения
├── docker-compose.yml     # Docker Compose конфигурация
├── .dockerignore          # Исключения для Docker
├── start.sh               # Скрипт запуска сервера (Mac/Linux)
├── start.bat              # Скрипт запуска сервера (Windows)
├── .htaccess             # Apache конфигурация
└── README.md
```

## Установка

1. Убедитесь, что у вас установлен PHP 7.4+
2. Клонируйте или скачайте проект
3. Запустите локальный сервер разработки (см. ниже)

## Запуск сервера разработки

### 🐳 Запуск с Docker (рекомендуется)

**Требования:**
- Docker Desktop (Mac/Windows) или Docker + Docker Compose (Linux)

**Быстрый запуск:**
```bash
# Запуск контейнеров
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

После запуска откройте:
- **Сайт:** http://localhost:8000
- **Админ-панель:** http://localhost:8000/admin
- **Adminer (БД):** http://localhost:8080

**Параметры подключения к БД в Adminer:**
- **Система:** MySQL
- **Сервер:** db
- **Пользователь:** autoparts_user
- **Пароль:** autoparts_pass
- **База данных:** autoparts_db

**Root доступ (для разработки):**
- **Пользователь:** root
- **Пароль:** rootpassword

**Полезные команды:**
```bash
# Пересобрать контейнеры
docker-compose up -d --build

# Войти в контейнер
docker exec -it autoparts-pro bash

# Войти в MySQL
docker exec -it autoparts-db mysql -u root -prootpassword autoparts_db

# Остановить и удалить все (включая volumes)
docker-compose down -v

# Просмотр статуса
docker-compose ps
```

**База данных MySQL:**
- **Host:** localhost:3306 (извне) или db (из контейнера)
- **Database:** autoparts_db
- **User:** autoparts_user
- **Password:** autoparts_pass
- **Root Password:** rootpassword

### 📦 Локальный запуск (без Docker)

**Mac/Linux:**
```bash
./start.sh
```

**Windows:**
```cmd
start.bat
```

**Или вручную:**
```bash
php -S localhost:8000
```

После запуска откройте в браузере: **http://localhost:8000**

> 💡 **Примечание:** Для остановки сервера нажмите `Ctrl+C` в терминале

## База данных

### Структура БД

Проект включает полную структуру базы данных для интернет-магазина:

- **categories** - Категории товаров (с поддержкой вложенности)
- **manufacturers** - Производители
- **products** - Товары (с полной информацией, SEO, рейтингами)
- **product_attributes** - Атрибуты товаров
- **product_attribute_values** - Значения атрибутов
- **users** - Пользователи (админы, менеджеры, клиенты)
- **user_addresses** - Адреса пользователей
- **orders** - Заказы
- **order_items** - Позиции заказов
- **cart_items** - Корзина
- **reviews** - Отзывы и рейтинги

### Оптимизация для больших объемов

- Индексы на всех ключевых полях
- Составные индексы для быстрого поиска
- FULLTEXT индекс для поиска по товарам
- Триггеры для автоматического обновления рейтингов
- Оптимизированные настройки MySQL для больших таблиц

### Импорт/Экспорт данных

Используйте Adminer для:
- Импорта CSV/Excel файлов
- Экспорта данных
- Массового редактирования
- Выполнения SQL запросов

## Админ-панель

Админ-панель доступна по адресу: **http://localhost:8000/admin**

### Возможности админ-панели:

- ✅ Дашборд с статистикой
- ✅ Управление товарами (в разработке)
- ✅ Управление категориями (в разработке)
- ✅ Управление заказами (в разработке)
- ✅ Управление пользователями (в разработке)
- ✅ Настройки (в разработке)

### Используемые библиотеки:

- **Bootstrap 5** - для основного сайта
- **AdminLTE 3** - готовая админ-панель
- **Font Awesome 6** - иконки
- **Bootstrap Icons** - дополнительные иконки

## Разработка

### Добавление нового контроллера

1. Создайте файл в `app/controllers/`, например `ProductController.php`:

```php
<?php
require_once 'BaseController.php';

class ProductController extends BaseController {
    public function index() {
        $productModel = $this->loadModel('Product');
        $products = $productModel->getAll();
        $this->renderView('products/index', ['products' => $products]);
    }
}
```

2. Добавьте маршрут в `index.php`:

```php
$router->addRoute('GET', 'products', 'ProductController', 'index');
```

### Добавление новой модели

1. Создайте файл в `app/models/`, например `Product.php`:

```php
<?php
require_once 'BaseModel.php';

class Product extends BaseModel {
    public function getAll() {
        return $this->getAll('products', ['is_active' => 1], 'created_at DESC');
    }
    
    public function getById($id) {
        return $this->getById('products', $id);
    }
    
    public function search($query) {
        $sql = "SELECT * FROM products 
                WHERE MATCH(name, description, short_description) AGAINST(:query IN BOOLEAN MODE)
                AND is_active = 1
                LIMIT 50";
        $stmt = $this->query($sql, ['query' => $query]);
        return $stmt->fetchAll();
    }
}
```

## Будущие улучшения

- [x] Интеграция Bootstrap 5
- [x] Административная панель (базовая структура)
- [x] База данных MySQL с Adminer
- [x] Структура БД для интернет-магазина
- [ ] Настройка базы данных
- [ ] Реализация аутентификации
- [ ] Корзина покупок
- [ ] Система заказов
- [ ] Полное управление товарами
- [ ] API для мобильного приложения
